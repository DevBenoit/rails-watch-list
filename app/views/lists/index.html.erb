<div class="container p-5">
  <h1>My lists</h1>
  <div class="row">
    <ul>
      <% @lists.each do |list| %>
        <li><%= list.name %>
        <%= link_to('View', list_path(list), class: "btn btn-link m-1") %></li>
      <% end %>
    </ul>
  </div>
    <%= link_to('Add a new list', new_list_path, class: "btn btn-outline-primary") %>
</div>

